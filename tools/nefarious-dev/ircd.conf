# Bind-mounted over /home/nefarious/ircd/ircd.conf in the nefarious2:ircv3 image.
# The image's own ircd.conf also includes linesync/gitsync files that nothing
# creates in a standalone dev container, and a missing include is a fatal
# config error.
include "base.conf";
include "local.conf";
